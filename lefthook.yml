pre-push:
  parallel: true
  commands:
    go-test:
      glob: "*.go"
      run: go test ./... -v
    go-vet:
      glob: "*.go"
      run: go vet ./...
    go-fmt:
      glob: "*.go"
      run: test -z "$(gofmt -l .)" || (echo "Run go fmt!" && exit 1)